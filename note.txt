24 febbraio ------------------------------------------------
http://daniele.tavagnacco.pages.ict.inaf.it/corso_lab_python/Lezione01/Lezione_1.html
per avviare l'interprete dal terminalescrivo corso_lab_pythonèer capire se l'interprete è stato avviato
al posto del $ devono apparire >>>
 domada d'esame tipica: quanti errori esistono in python?
 risposta 3: Syntax errors, semantic errors, runtime errors + descrizione
 import istruzione per importare delle istruzione scritte altrove
 l'assegnazione non può essere invertita
 per controllare che due variabili dello stesso tipo non uso == ma uso istruzioneper chiudere e salvare:
 nel terminle:
    git status, per verificare se cisono modifiche non salvate
    git add . per salvare le modifiche
    git commit -am "nomefile", per salvare il file con nomefile
    git push origin main, per spostare tutti i file nel main (che è il posto incui vogliamo tenere i nostri file)

3 marzo ----------------------------------------------------
http://daniele.tavagnacco.pages.ict.inaf.it/corso_lab_python/Lezione02/Lezione_2.html
python linguaggio interpretato non compilato
abbiamo creato il file turtle.py
python costringe i programmatori a scrivere bene e utilizza la trutturaa blocchi
essa èformata da dei rientri dopoche viene definito un blocco, più rientri=più blocchi
ATTENZIONE se si utilizzano i rientri in modo errato ne risulta un erroredi tipo runtime
nel while devo definire io la condizione di uscita
lafunzione for con loop su numeri interi è così tanto usato che esistono dverse scorciatoie esempio: range (num) essa p
arte da0 arriva a num-1 e fa step di 1 unità 
condizione if è una boolean expression, strutturata in modo tale che se condizione true esegue le istruzioni
immediatamente successive, altrimenti se FALSE esegue l'else

12 marzo--------------------------------------------------
http://daniele.tavagnacco.pages.ict.inaf.it/corso_lab_python/Lezione03/Lezione_3.html
le funzioni le posso dichiarare ovunque io vogliamo
def draw_sqare(my_turtle, step):    #le funzioni le posso dichiarare dovunque io voglia nel codice
    '''Disegno un quadrato con la tartaruga''' #il commento multi linea esattamente posto sotto definizione di funzione  diventa 
    una stringa di documentazione, quindi non è più trasparente nel codice ma crea codice
#qusto tipo di stringa è importante per comprensione e portabilità del codice, 
#pass è una keeword che ci permette di definire funzione vuota senza che il codice dia errore
ATTENZIONE: sono molto pignoli sulla presenza delle Doc string
le funzioni possono essere chiamate solo dopo aveerle definite, per questo è buona procedura inizializzare tutte le funzioni 
all'inizio
ESERCIZIO 1

17 marzo--------------------------------------------------
http://daniele.tavagnacco.pages.ict.inaf.it/corso_lab_python/Lezione04/Lezione_4.html
la striga va sempre con gli ''
TypeError è un errore di runtime
l'oggetto di tipo intero non ha lunghezza
Stringa composta da più elementi, quindi posso gurdenrne uno alla volta mediante l'utilizzo dell'operatore di indicizzazione
in python posso contare anche zero, quindi non solo posso scrivere String1[0], che mi da il primo valore da sinistra
ma posso anche partire dal fondo, ovvero utilizzando String[-1] e quindi mi restituisce il primo valore da destra
per avere il secondo elemento da destra basterà scrivere String[-2] e così variabili
si possono fare azioni sulla stringa mediante la dot notation
ogni volta che faccio modifiche a stringhe in realtà la sctringa non viene modificata ma viene fatta una copia, lasciando intatta
l'originale
questa nuova copia della stringa non vine assegnata a nessuna variabile, quindi se ci dimentichiamo di assegnarla verrà 
considerata presenza quindi dobbiamo fare a=nome.upper()
help utile per confermare ciò che ha detto IA 
per scorrere ogni carattere della stringa e stamparlo non è necessario selezionare ogni elelemto, unio alla volta e poi stamparla,
basta digitare :
for lettera in nome:
    print(lettera)
per convertire un tipo primitivo in tipo stringa uso operatore di cart: str(variabile)
la concatenazione è il metodo più semplice nel unire più stringhe ma sposta tutta la responsabilità
sul programmatore, in quel caso è meglio usare l'operatore print e affidare tutta la responsabilità a linguaggio
l'old-style ha ancora delle limitazioni: mi devo ricordare che tipo di variabile inserisco, e devo ricordarmi l'ordine delle 
variabile che inserisco 
dopo la stringa tra marente, perchè dev3e corrispondere all'ordine incui appaiono dentro la stringa
nel new-style posso evitae i %tipovar inserendo {} e se all'interno delle paentesi graffe inserisco il nome della variabile
posso dopo evitare di doverle inserire in ordine quando passo alla stringa le variabili, devo solo ricordarbi il nome presente 
dentro le {} ed assegnarli il valore corretto
le più recenti sono le fstring ed inizianon con f', in cui rimuovo tutto ciò che c'era sta a contorno inserisco soloe {} nel 
punto in cui voglio inserire la variabile e all'interno delle {} inserisco il nome della variabile, o un qualunque tipo di espressione
le liste sono oggetti modificabili
le liste vuote si possono costruire in due modi:
        usando le parentesi quadre: lista_vuota=[]
        oppure con il casting a lista di niente: lista_vuota_2 = list( )
nello slice (rimozione elemetni) delle liste ti prende l'elemento iniziale segnato fino all'elemento prima dell'elemento finale segnato 
lista[0:3] da elemento 0 a elemtno 2
nelle liste ci sono 2 operatori
     + che è la concatenazione
     * che è la replica
tuple gruppi di elementi che non possono essere modificati una volta creati
Tuple assignment: una tupla di valori, posso assegnare i valori a una tupla di variabili, mappando quindi valori,
Ma il numero di variabili deve essere lo stesso del numero di valori
DOAMANDA A TRABOCCHETTO:
quando scrivo una funzione, ci ricordiamo che la funzione iniza con def nome (variabili che utilizza)
e voglio creare una funzione che restituisce la somma e la differenza tra due numeri, MA le funzioni possono restituire un solo 
elemento, quindi oer ritornare più di un elemnto di una funzione posso utilizzare una tupla ceh contine i due risultati e la ritorno
e quando uso una funzione nel main gli metto davanti una tupla di due variabili in questo utilizzo il tuple assignment, quindi 
LA FUNZIONE PUò RESTITUIRE UN VOLORE E BASTA, SE VOGLIO RESTITUIRE PIù VALORI DEVO UTILIZZARE UNA tupla
python nasce con le tuple quindi le () sono opzionali
nei set l'ordine in cui sono posti gli elementi viene deciso da pyton a seconda della memoeria che ha a disposizione
i set non sono modificabili 
ELEMENTI NON MODIFICABILI NON POSSONO CONTENERE AL LORO INTERNO ELEMENRTI MODIFICABILI 
nei set non essendo ordinati per accedere agli elemneti non posso accedere con gli indici ma devo nutilizzare dei loop o prendere 
gli elementi uno alla volta

RIASSUNTO/RECAP DI QUELLO CHE ABBIAMO VISTO:
abbiamo visto
tipi semplici: interi, float, etc
tipi composti: compound data Type,  sono più o meno uguali, ma con delle piccole differenze
    si parte dal tipo più largo, le liste: sono modificabili, possono contenere qualunque altro elemento, anche altre liste
    si restringe il campo, le tuple: possono contenere qualunque elemento, ma NON sono modificabili
            se poi imponiamo che nella tupla tutti gli elementi siano uguali allora abbiamo una stringa 
    i set sono un mondo un po' a parte: non modificabili e vengono utilizzati per fare cose particolare, es. operazioni di insiemistica

i dizionari: sono un tipo composto particolare perchè sono una mappa, sono coppie di oggetti (chiave:valore, chiave:valore, etc...)
per accedere al valore utilizziamo [chiave], se chiave non c'è si ha un errore di RUNTIME 
si possono fare loop sia sulle chiavi che sui valori, di default si fanno sulle chiavi ma per farlo sui valori bisogna dirlo 
in modo esplicito ovvero:
sulle chiavi: 
for el in inventario.keys():
    print(el)

che equivale ad 
for el in inventario:
    print(el)

sui valori:
for el in inventario.values():
    print(el)

la chiave può essere qualunque cosa purchè sia un oggetto non modificabile, i valori possono essere qualunque cosa
quando modifico il valore associato ad una chiave esso piò essere un elemento anche diverso dal valore iniziale
COMPREHENSION: sonon il modo in cui si generano e scorrono questo tipo di dati (list,set dict) in python
                OSSERVA:le tuple sono le uniche che NON hanno una comprehension 
modo più efficente e voleco per creare una list comprehension è utilizzando un cotruttore [ ] che sfrutta il concetto di loop for, che è scritto
all'interno di python e NON dall'utente
per creare un set comprehension utilizzo al posto delle [ ] le { }
per creare una dir comprehension utilizzo {} utilizzando le coppie di elementi quindi ad esempio {i:i*i  for i in range(10)}
che però si può scrivere anche str(i):i*i  for i in range(10)

24 marzo ----------------------------------------------------------------------------------
http://daniele.tavagnacco.pages.ict.inaf.it/corso_lab_python/Lezione05/Lezione_5.html
chiedono sempre all'esame cos'è import: è un'istruzone  che carica moduli o librerie esterne in memoria, permettendo di riutilizzare codice 
preesistente e organizzare il codice in modo strutturato
esempio sys mi permette di fornire il dato da terminale e passargli informazione mantendo il programma generale
mediante la seguente struttura $ python3 programma.py  argomento1  argomento2 
è importante ricordarsi l'ordine esatto, altrimenti vengono utilizzate in modo errato nel programma, 
soprattutto se dobbiamo passare mote informazioni
parser =  argparse.ArgumentParser() si occupa di analizzare l'input che forniamo, lo si può utilizzare importando la 
libreria import argparse
per far si che funzione bisogna registrare gli argomenti che forniremo uno a unogli argomenti li aggancio a parser e per farlo devo dare un 
nome ad ogni argomento
ad esempioparser.add_argument('-n','--nome_esteso', help='Descrizione del parametro')
parser.add_argument('-b','--boolean_value', action='store_true', help="imposta il valore 'True' se trova il parametro")
parser.add_argument('-d','--con_default',  default='riferimento', help="Parametro che ha già un valore di default se non viene fornito l'argomento")
per leggere gli argomenti devo chiamare la funzione: args = parser.parse_args() # fa il parsing degli argomenti da linea di comando
per poi accedere agli argomenti forniti da terminale utilizzo la dot notation

31 marzo ----------------------------------------------------------------------------------
readline legge solo una riga
readlines legge tutto il file dividendole per righe e leggendole una alla volta come stringhe, per poi inserendole in una lista
read legge tutto in un'unica enorme stringa
FileNotFoundError: Errore di runtime, hanno molto nomi oer far capire in che categoria rientrano
if len(linea) == 0:                          
        break
potrebbe andar storto nel momento non vi è nemmeno il carattere a capo, quindi una riga completamente vuota,
fortunatamente readline legge anche i caratteri speciali, quindi se c'è il carattere \n if non viene eseguito
iport jeson carica tutto dentro la libreria 
crea struttura del dizionario annidato con associazione chiave valore

14 aprile ----------------------------------------------------------------------------------
http://daniele.tavagnacco.pages.ict.inaf.it/corso_lab_python/Lezione07/Lezione_7.html
uso una lista di liste annidate percreare la mia scacchiera
r + c == riga + colonna and scacchiera[r][c] == 1:  è il coefficente angolare tra 2 rette 
per conrolare l'altra diagonele cambio segno:  if r - c == riga - colonna and scacchiera[r][c] == 1: 
tanti casi possibili
nella matrice 8x8 ci sono tanti zeri (nessuna informazione) e pochi 1 (ciò che contiene l'informazione)
quindi posso passare da una matrice 8x8 a una lista di 8 coordinate che indicano le posizione delle regine, le coordinate sono 
le informazoni riga,colonna
usando una rappresentazione per riga
si può osservare che le righe vanno da 0 a 8 in modo ordinato mentre le colonne non saranno niente altro che una lista 
dei punti diversi da 0 per ogni riga,diventa quindi una lista diposizioni
quindi le soluzioni non sono altro che delle permutazioni di quei 8 numeri, che sono calcoli fattibili per un computer in poco 
tempo
èda verificare che la ermutazione non capiti sulla stessa diagonale, quindi non devono avere lo stesso coefficente angolare
ECCEZIONI PARTCOLARI: le scecchiere 1x1 e 2x2 non hanno soluzione
per tutte le altre scacchiere si riescono a trovare delle soluzioni, per una scacchiera 8x8 ci sono solo 92 combinazioni possibili 
e il computer deve svolgere in media 438 tentativi per trovare una soluzione ammissibile 

DOMANDA TIPICA D'ESAME: 'cosa succede quando faccio import random?'
random è un modulo
python va a cercare una libreria che si chiama random all'intern delle sue librerie, importa tutto quanto il codice scrittonella 
libreria dentro ad un namespace a cui da il nome random, quindi er usare tutto ciò che rigurda random devo usare la dotnotation
randomè un modulo che devo inizializzare, quindi creo l'oggetto:
# oggetto Random
random_generator = random.Random()
shuffle, non restituisce una nuova lista con gli elementi rimascolai, ma mescola direttamente gli elementi della lista e si 
eprdela lista oriinaria

NOTA: ogni volta che trovo una soluzione valida delle 8 regine, mi basta ruotare di 90°, 180°, 270° per trovare altre 3 soluzioni 
ammissibili. Quindi con 1 soluzione in realtà ne trovo 4 per rotazione

DOMANDA D'ESAME:'quanti tipi di comando si possono dare a python?'
2 tipi di comando: gli statement ele espressioni
    - gli statement: NON RITORNA RISULTATO (es. def shuffle, i def, import dono statement)
    - le espressioni: RITORNA RISULTATO (chiamate a funzione sono espressioni, quindi è una riga di effettivo comando)